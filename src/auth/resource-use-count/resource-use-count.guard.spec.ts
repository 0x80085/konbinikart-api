import { ResourceUseCountGuard } from './resource-use-count.guard';

describe('ResourceUseCountGuard', () => {
  it('should be defined', () => {
    expect(new ResourceUseCountGuard()).toBeDefined();
  });
});
