import { AiTranslationHandler } from './ai-translation-handler';

describe('AiTranslationHandler', () => {
  it('should be defined', () => {
    expect(new AiTranslationHandler()).toBeDefined();
  });
});
